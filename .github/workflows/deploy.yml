jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build site
        run: |
          echo "window.GITHUB_TOKEN='${{ secrets.GITHUB_TOKEN }}';" > token.js
          # Continue building HTML or JS
